/* mm/memory.c */

#ifndef MM_MEMORY_H
#define MM_MEMORY_H

#include <memory.h>

void memset(void* dst, int val, size_t count)
{
    // int len = count / sizeof(dst[0]);
    // for(int i = 0;i < len;i++)
    // {
    //     dst[i] = val;
    // }
}

#endif